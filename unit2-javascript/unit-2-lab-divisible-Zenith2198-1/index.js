// Follow the comments to complete the exercise

// Declare your numbers to test
const dividend = 12434536745;
const divisor = 5;

// Calculate the remainder using the modulo operator
const remainder = dividend % divisor;

// Print the remainder to verify your calculation
console.log(remainder)

// Using an if...else statement check if the numbers are evenly divisible
if (remainder === 0) {
	console.log(true);
} else {
	console.log(false);
}